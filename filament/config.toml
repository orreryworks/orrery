# Filament default configuration file

# Layout engine configuration
[layout]
# Default layout engine for component diagrams (basic, force, sugiyama)
component = "force"
# Default layout engine for sequence diagrams (basic)
sequence = "basic"